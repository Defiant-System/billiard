
let Test = {
	init(APP) {

		// setTimeout(() => APP.spinSetter.dispatch({ type: "open-spin-setter" }), 500);

		// setTimeout(() => {
		// 	APP.game.dispatch({ type: "game-stop" });
		// }, 1000);

	}
};
